[project]
name = "ai-utils"
version = "0.1.0"
description = ""
authors = [
    {name = "pathikrit",email = "pathikritbhowmick@msn.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "ai_utils", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
